/* Phase 2: advisor portal + operations panels (PRD.md §5). Farmer product is the Flutter app. */

export default function Home() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <div className="text-center">
        <h1 className="text-2xl font-semibold">Kisaan Dost — Web Dashboard</h1>
        <p className="mt-2 text-sm text-gray-500">
          Advisor portal and operations panels arrive in Phase 2.
        </p>
      </div>
    </main>
  );
}
