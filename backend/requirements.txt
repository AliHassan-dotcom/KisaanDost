# Kisaan Dost API — Python 3.12
# Lock exact versions for staging/prod (pip-compile / uv) — RULES.md §7.7

# API
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.11
pydantic-settings>=2.9

# Database
sqlalchemy[asyncio]>=2.0.40
asyncpg>=0.30
geoalchemy2>=0.17
alembic>=1.15

# Geo / AOI validation
shapely>=2.1

# Cache & background jobs
celery[redis]>=5.5
redis>=5.2

# External integrations
httpx>=0.28
earthengine-api>=1.4  # workers only — never install in client builds

# Auth
pyjwt>=2.10

# Observability
structlog>=25.1
sentry-sdk[fastapi]>=2.25

# Dev / test
pytest>=8.3
pytest-asyncio>=0.26
factory-boy>=3.3
ruff>=0.11
mypy>=1.15
